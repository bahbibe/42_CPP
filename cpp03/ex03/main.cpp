#include "FragTrap.hpp"
int main( void )
{
	FragTrap b;

	b.highFivesGuys();
	// ScavTrap a("random");
	// a.attack("you");
	// a.takeDamage(10);
	// a.takeDamage(10);
	// a.beRepaired(10);
	// a.takeDamage(10);
	// a.takeDamage(10);
	return 0;
}
